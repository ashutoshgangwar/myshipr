import type {Option} from '../types/common';

/** The organisation roles the signup form offers. */
export const Roles: Option[] = [
  {label: 'Carrier', value: 'carrier'},
  {label: 'Admin', value: 'admin'},
];
